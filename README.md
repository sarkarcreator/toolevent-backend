# Toolbox.Events Backend

Next.js 15 backend/API for Toolbox.Events.

## Local development
1. Copy `.env.example` to `.env` and configure secrets.
2. `npm install`
3. `npm run dev -- -p 3001`

API routes are under `app/api`.
